<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
    
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
    <div class="max-w-3xl mx-auto p-4">
        <h1 class="text-3xl font-bold mb-4">Available Books</h1>
        <ul>
            <% books.forEach(book => { %>
                <li class="bg-white rounded-lg shadow-md p-4 mb-4 flex items-center justify-between">
                    <div>
                        <p class="text-xl font-bold"><%= book.title %></p>
                        <p class="text-gray-600"><%= book.author %></p>
                        <p class="text-gray-700 mt-2">$<%= book.price %></p>
                    </div>
                    <div class="flex space-x-4">
                        <form action="/books/<%= book.id %>/delete" method="POST" style="display: inline;">
                            <button type="submit" class="bg-indigo-500 hover:bg-indigo-600 text-white font-bold py-2 px-4 rounded">Delete</button>
                        </form>
                        
                        <a href="/books/<%= book.id %>/update" class="bg-indigo-500 hover:bg-indigo-600 text-white font-bold py-2 px-4 rounded">Update</a>
                    </div>
                </li>
            <% }); %>
        </ul>
        <a href="/newBook" class="inline-block bg-indigo-500 hover:bg-indigo-600 text-white font-bold py-2 px-4 rounded">Add Book</a>
    </div>
</body>
</html>
