<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Home</title>
</head>
<body>
  <h1>Available Books</h1>
  <ul>
    <% books.forEach(book => { %>
      <li><%= book.title %> by <%= book.author %> - $<%= book.price %></li>
    <% }); %>
  </ul>
  <a href="/newBook"><button>Add Book</button></a>
</body>
</html>
